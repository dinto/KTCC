instructions:
1)once team click start  bid then team will be inside bid competition
2) admin should add all team login from admin dashboard
3)icon player = 10000 hence set maximum point become 40000  for particular team & icon player before bid date

check/testing:
1)remove or add to bid details  bid_screen_new player if greater than 2    problem when admin refresh the screen
2)delete of foreign field working or not 
3)malayalam 

work:
not emergency work:
1)Tournment details like youtube link ,facebook link,insta,phone ,email id  from database to base template
2) page not found setting 
3)Reset for Live -> it will delete bid bucket and bid players   before this one warning msg needed
4)admin can lock bidding by team
5)instructions typing model if possible
6)all bid entries for player add as history for looking for everybody 

1)disable players registration button as well as link
2)messages not displaying 
3)page not reloading  during start bid and bid increment (refresh bid screen)
4)pdf work
5)swap player option ( cancelled player beome unavailable and reason typing) update both registered and bid database
6)my team

work pending:
1)design work & mobile view
2)hosting part
3)pdf work


==========
Team bid status:
teamlogo 
team name 
No of players 
remaining
max 50000  



workflow:

Bid_Screen_new_Player : bucket>0 ,if yes then current player true for random number else -> save playersinfo to bid player model and  current player is false
bid screen : print all current player true

rules:
player registration in particular time period
swap player if any required
if 15 members in 1 team then admin can disallow for biding 



https://www.youtube.com/watch?v=I5x8lAVQ8QQ

vercel:
both vercel.json & build_file.sh create inside root directory.

========
build_files.sh
--------------
pip install -r requirement.txt
python3.10 manage.py collectstatic

========
vercel.json:
------------------

{
    "builds": [{
        "src": "Kerala_Theaters_Cricket_club/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    },
    {
        "src": "build_files.sh",
        "use": "@vercel/static-build",
        "config": { "distDir": "staticfiles_build" }
    }],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/(.*)",
            "dest": "Kerala_Theaters_Cricket_club/wsgi.py"
        }
    ]
}

========
wsgi.py
-----------
app=application

============
settings
----------
STATICFILES_DIRS=os.path.join(BASE_DIR,'static'),
STATIC_ROOT= os.path.join(BASE_DIR,'staticfiles_build','static')

DATABASES={
 'default':{
'ENGINE':'django.db.backends.postgresql',
'NAME':'',
'USER':'',
'PASSWORD':'',
'HOST':'',
'PORT':'6625',


}

}

ALLOWED_HOSTS=['.now.sh','.vercel.app']
#debug=false

====================
urls.py
---------------
from django.conf import settings
from django.conf.urls.static import static
urlpatterns +=static(settings.STATIC_URL,document_root=settings.STATIC_ROOT)

========================
requirement.txt:
..................
remove below name:
psycopg2




=============================================
aws ec2 configuration note:
==========================================
sudo apt-get update
sudo apt-get upgrade
enter for prompt screen

clear
python3 --version

virtual env pack:

sudo apt-get install python3-venv
enter y then enter in prompt

python3 -m venv virenv

source virenv/bin/activate 
git clone https://github.com/dinto/KTCC.git

sudo apt-get install -y nginx
enter in prompt

pip install gunicorn
pip list

check nginx printing in 3.108.236.40 url

install supervisor (it helps application running background smoothly)

sudo apt-get install supervisor
enter in prompt

cd /etc/supervisor/conf.d/

sudo touch gunicorn.conf

sudo nano gunicorn.conf:



[program:gunicorn]
directory=/home/ubuntu/KTCC
command=/home/ubuntu/virenv/bin/gunicorn --workers 3 --bind unix:/home/ubuntu/KTCC/app.spck  Kerala_Theaters_Cricket_club.wsgi:application
autostart=true
autorestart=true
stderr_logfile=/var/log/gunicorn/gunicorn.err.log
stdout_logfile=/var/log/gunicorn/gunicorn.out.log
[group:guni]
programs:gunicorn


to save clt+o 
clt+ x to exit

sudo mkdir /var/log/gunicorn

sudo supervisorctl reread

sudo supervisorctl  update
sudo supervisorctl  status

cd /etc/nginx
sudo nano nginx.conf
change user =>
user=root; 


cd sites-available
sudo touch django.conf
sudo nano django.conf

server{
	listen 80;
      server_name 3.108.236.40;
      location / {
		include proxy_params;
		proxy_pass http://unix:/home/ubuntu/KTCC/app.sock;
      }

}

sudo nxinx -t
sudo ln django.conf /etc/nginx/sites-enabled
sudo service nginx restart




=======